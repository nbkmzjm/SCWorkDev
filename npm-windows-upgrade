#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/node_modules/npm-windows-upgrade-portable/bin/npm-windows-upgrade" "$@"
  ret=$?
else 
  node  "$basedir/node_modules/npm-windows-upgrade-portable/bin/npm-windows-upgrade" "$@"
  ret=$?
fi
exit $ret
